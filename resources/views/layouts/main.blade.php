<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
        <!-- metas -->
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        
        <!-- titulo -->
        <title>{titulo conteudo} - Central</title>

        <!-- scripts -->

        <!-- tags/etc -->
    </head>
    <body>

        <!-- conteudo -->
        @yield('content')

    <!-- footer -->

    <!-- scripts -->